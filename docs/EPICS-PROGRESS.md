# Bursa - Autonomer Epic-Build (Fortschritts-Anker)

Dieses Dokument ist die Quelle der Wahrheit fuer den autonomen Build aller 5 Epics.
Es ueberlebt Kontext-Kompaktierung. Nach einem Reset: hier lesen, dann am offenen Schritt weitermachen.

**Direktive (Dennis, 2026-06-27):** Alle 5 Epics in Reihenfolge E1->E2->E3->E4->E5 mit vollem
spec-kit-Flow, voll autonom orchestriert, OHNE Zwischen-Input. Ziel: alle robust, fehlerfrei,
gemaess Constitution.

## Entscheidungen (am Anfang geklaert - gelten fuer ALLE Epics)

1. **E2 Payments:** Mock (Default, laeuft ohne Keys) **+** echter Stripe-Test-Adapter hinter
   Env-Flag `PAYMENT_PROVIDER=stripe|mock`. Beide implementieren dieselbe PaymentProvider-Abstraktion.
2. **Tests:** Volles TDD, **80%+ Coverage** pro Epic. Thresholds in der Test-Config erzwingen
   (Backend Jest `coverageThreshold`; Frontend mit Coverage messbar machen - jest-preset-angular
   oder Karma headless). Tests zuerst (RED), dann Implementierung (GREEN), dann Refactor.
3. **Git:** Feature-Branch + PR pro Epic, **gestapelt** (E(n) branched von E(n-1)). PR-Base =
   Vorgaenger-Branch -> saubere Per-Epic-Diffs. NICHT selbst mergen; Dennis mergt spaeter in
   Reihenfolge E1..E5. Branch-Naming: `00X-<slug>` (spec-kit-Feature-Nummer).
4. **Infra-Defaults:** Video = Embed per URL (kein File-Hosting). E-Mail/Notifications = In-App +
   gemockt/geloggt (kein SMTP). Recurring = ueber Payment-Engine simuliert. Reporting = In-App +
   CSV/PDF-Export. Keine neue externe Infra.

## Per-Epic-Pipeline (spec-kit + TDD)

1. `git checkout -b 00X-<slug>` (von Vorgaenger-Branch bzw. main fuer E1)
2. spec-kit Feature anlegen (`create-new-feature.sh`) -> `specs/00X-<slug>/`
3. **spec.md** (User Stories, FR, Key Entities, Success Criteria) - selbst, informiert durch
   `docs/roadmap-epics.md` + Recherche-Docs
4. **clarify** - Offene Fragen SELBST recherchieren/entscheiden (kein User-Input), in research.md dokumentieren
5. **plan.md** + research.md + data-model.md + contracts/api.md + quickstart.md
6. **tasks.md** (TDD-geordnet: Tests vor Implementierung)
7. **implement (TDD)** - orchestriert mit Sub-Agents/Workflows; Tests zuerst, dann Code, Coverage >=80%
8. **verify** - Build gruen + alle Tests gruen + Coverage >=80% + Playwright-E2E des Epic-Flows
9. **commit + push branch + PR** (gh pr create, Base = Vorgaenger-Branch)
10. Diesen Tracker + Statustabelle aktualisieren -> naechstes Epic

App bleibt nach JEDEM Epic lokal lauffaehig + deploybar (Migrationen committed, Seed aktualisiert).

## Die 5 Epics (Detail in docs/roadmap-epics.md)

- **E1 - Trust-Layer sichtbar machen** (Verification-Badges + Direktfluss-Beweis) -> Feature 002
- **E2 - Stripe All-or-Nothing Zahl-Engine + Goal-Mechanik** (Mock + Stripe-Adapter, Kampagnen-States) -> 003
- **E3 - Kampagnen-Erfolgs-Engine** (Story/Video-Embed, Onboarding-Split, Share-Toolkit) -> 004
- **E4 - Donor Retention Loop** (Account, Recurring-simuliert, Impact-Updates, Tribute) -> 005
- **E5 - Corporate Channel** (B2B-Payment, ESG-Dashboard, Named Scholarship, Full-Tuition-CTA, CSV/PDF) -> 006

## Phase 2 (Folgeauftrag, nach E5 - Dennis 2026-06-27)

Nach Abschluss von E1-E5: einen weiteren Stakeholder-Research-Workflow starten fuer die **4.
Stakeholder-Gruppe: Entwickler/Betreiber der Plattform**. Daraus **2 Epics** definieren (Kandidaten:
Security-Hardening, Analytics/Observability, Marketing/SEO/Growth, oder aehnlich) -> Features `007`
und `008`. Diese 2 Epics dann ebenfalls VOLL AUTONOM mit demselben spec-kit+TDD-Flow umsetzen
(gleiche Entscheidungen wie oben: 80% Coverage, Branch+PR pro Epic, pragmatische Defaults).
Reihenfolge gesamt: E1->E2->E3->E4->E5->[Research devs/ops]->E6->E7.

## Status

| Epic | Feature | Branch | Spec | Plan | Tasks | Impl | Tests>=80% | E2E | PR | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| E1 | 002 | 002-trust-layer | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #1 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/1 |
| E2 | 003 | 003-payments-allornothing | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #2 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/2 |
| E3 | 004 | 004-campaign-success | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #3 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/3 |
| E4 | 005 | 005-donor-retention | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #4 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/4 |
| E5 | 006 | 006-corporate-channel | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #5 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/5 |
| E6 | 007 | 007-security-hardening | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Live-Smoke + Komponententests) | #6 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/6 |
| E7 | 008 | 008-observability-analytics | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Live-Smoke + Komponententests) | #7 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/7 |
| E8 | 009 | 009-school-portal | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #8 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/8 |
| E9 | 010 | 010-trust-safety | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #9 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/9 |
| E10 | 011 | 011-ai-coach | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #10 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/10 |
| E11 | 012 | 012-kyc-verification | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | #11 | **FERTIG** - PR https://github.com/slicedelephant/bursa/pull/11 |
| E12 | 013 | 013-payout-reconciliation | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E13 | 014 | 014-employer-matching | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E14 | 015 | 015-csrd-reporting | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E16 | 016 | 016-donor-portfolio | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E15 | 017 | 017-referral-engine | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E17 | 018 | 018-impact-feed | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E18 | 019 | 019-groups-engine | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E19 | 020 | 020-scholarship-manager | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |
| E20 | 021 | 021-multi-currency | ✓ | ✓ | ✓ | ✓ | ✓ (neuer Code, Per-Path-Gates) | (Komponententests) | (offen) | **FERTIG** - PR offen |

## Wichtige Infra-Notizen (fuer alle Epics)
- **Frontend-Tests jetzt Jest** (jest-preset-angular, jsdom/headless), nicht mehr Karma. Run:
  `npm --prefix apps/web test` bzw. `test:cov`. Coverage-Gates pro Pfad in `apps/web/jest.config.js`
  (global floor 0). Fuer jedes Epic die neuen Pfade als 80%-Gate dort eintragen.
- **Backend-Tests:** `npm --prefix apps/api test` (Jest, coverage via test:cov).
- **Coverage-Interpretation:** 80% gilt auf dem NEUEN/geaenderten Code je Epic (Per-Path-Gates),
  nicht global (Altbestand aus Nacht-Build ist untested - globaler Retrofit waere separat).
- Verify pro Epic: `npm --prefix apps/api test` + `npm --prefix apps/web run test:cov` + beide `run build`.

## Log
- 2026-06-27: Direktive + E6/E7-Folgeauftrag erhalten, Entscheidungen geklaert, Tracker angelegt.
- 2026-06-27: **E1 FERTIG** (PR #1). Backend Trust-Projektion + Trust-UI + Karma->Jest-Migration. Naechster: E2 (Branch 003 von 002-trust-layer).
- 2026-06-27: **E3 FERTIG** (PR #3). Kampagnen-Erfolgs-Engine: Video-Embed per URL (YouTube/Vimeo, kein Upload) via purem video-url.util + @IsEmbeddableVideoUrl-Boundary-Validator + responsivem campaign-video (nocookie iframe). Geführtes Story-Framework (3 Vorher/Nachher-Prompts -> komponierte Story). Mehrstufiger Onboarding-Wizard (Basics->Story->Video/Review) mit Fortschritt + localStorage-Autosave (Zwischenspeichern), ersetzt das Single-Shot-Formular. Ein-Tap Share-Toolkit (WhatsApp/Telegram/Facebook-Deeplinks + Copy, EN/DE-Texte, mobile-first, "erste 3 Spender"-Framing). Prisma campaign_story_video (+videoUrl/storyBackground/storyChallenge/storyVision), Seed mit Beispiel-Videos. API 76 Tests, Web 102 Tests grün, Per-Path-80%-Gates, beide Builds grün, Seed läuft. Bewusst client-seitiges Zwischenspeichern (localStorage) statt server-seitigem Draft, um den geprüften Create/Verify-Pfad nicht zu destabilisieren. Naechster: E4 (Branch 005 von 004-campaign-success).
- 2026-06-27: **E4 FERTIG** (PR #4). Donor Retention Loop: Spender-Account (`/donor`) mit Spendenhistorie, Zuwendungsbelegen und Lifetime-Impact-Summary; Kartenspende wird via `OptionalJwtAuthGuard` eingeloggten DONOR-Konten zugeordnet (anonyme Spende bleibt möglich). Recurring = SIMULIERT über die PaymentProvider-Engine ("Sponsor a Student", Pause/Resume/Cancel + "Simulate next charge"), bewusst entkoppelt vom geprüften E2-All-or-Nothing-Capture-Pfad (eigener Sofort-Charge analog Corporate-SEPA). Impact-Updates als In-App-Feed + geloggte E-Mail (EmailLogger, kein SMTP, kein console.log): Update des Studierenden -> Fan-out an alle Abonnenten; Auto-Dank nach Spende; Meilenstein-Notifications bei 80/90/100%. Tribute/Widmung ("zu Ehren von"/"im Gedenken an") am Boundary validiert (cross-`@ValidateIf`), Anonymität erhalten. Prisma-Migration `donor_retention` (RecurringPledge/UpdateSubscription/Notification + Donation-Felder). API 152 Tests, Web 129 Tests grün, Per-Path-80%-Gates auf allen neuen Dateien, beide Builds grün, Seed um Donor-Retention-Demodaten erweitert + lauffähig. Frontend: Donor-Account-Seite, pure Helfer (donor-summary/notification-format/tribute-display) + Komponenten (notifications-feed/donation-history/recurring-list) gegated; donate-card um Tribute + Monatlich-Toggle erweitert; impact-update-form im Studi-Dashboard. Naechster: E5 (Branch 006 von 005-donor-retention).
- 2026-06-27: **E5 FERTIG** (PR #5). Corporate Channel: B2B-Sponsoring-Pfad als eigenes `corporate`-Modul (analog Recurring), berührt den geldkritischen E2-Spenderpfad nicht. Full-Tuition-CTA "Lücke schließen" mit exaktem Restbetrag + Gift-Tiers (Semester/Jahr/Komplett); Karte = sofortiger Capture über neues `PaymentProvider.chargeImmediately` (automatic capture, Mock + Stripe) → Kampagne sofort FUNDED, kein All-or-Nothing-Wait. Schließt ein Corporate-Ticket die Lücke zu 100%, werden die offenen Spender-Pledges via getestetem `DonationsService.captureCampaign` mitgecaptured (binäre AoN-Auszahlung gesichert). SEPA als Konzept gemockt (Zusage sofort SUCCEEDED, Rechnung PENDING → `settle` → PAID) mit USt-ID + PO. Beleg-Verzweigung: ohne Logo = Zuwendungsbestätigung (keine USt), mit Logo-Recognition = Sponsoring-Rechnung mit 19% USt (Netto bleibt 100% schulgebunden, USt nur Aufschlag). Named Scholarship + Logos als Recognition-Banner auf der Kampagne. ESG-/CSR-Dashboard im Sponsor-Account (Studierende/Länder/Schulen/Summe/Voll-/Named-Stipendien) + CSV- und PDF-Export (eigener dependency-freier Mini-PDF-Writer mit korrekten xref-Offsets, keine neue Infra). Prisma-Migration `corporate_channel` (CorporateSponsorship + Invoice + Enums). Pure Kerne (gift-tiers/invoice-VAT/esg+csv/pdf/recognition) als Coverage-Rückgrat. API 203 Tests, Web 154 Tests grün, Per-Path-80%-Gates auf allen neuen Dateien, beide Builds grün, Seed um Named-Scholarship-Sponsorship + Rechnung erweitert + lauffähig. Letzter der 5 Epics — Kette E1->E5 fertig. Naechster (Phase 2): Stakeholder-Research devs/ops -> E6/E7.
- 2026-06-27: **E6 FERTIG** (PR #6, Phase 2). Security-Hardening als eigenes `security`-Modul, bewusst OHNE neue externe Infra (in-memory, dependency-frei — Linie des E5-PDF-Writers). Rate-Limiting: purer Fixed-Window-Store + globaler `RateLimitGuard` + `@RateLimit()` auf Login (5/60s)/Registrierung (5/300s)/Donation-Card (10/60s) → `429` + `Retry-After`/`X-RateLimit-*` (Brute-Force-/Card-Testing-Bremse). Security-Header (CSP/HSTS/nosniff/DENY/Referrer/Permissions) als pure Funktion + Middleware (HSTS nur Prod). `validateEnv` fail-closed in Prod (Default-/zu-kurzes JWT_SECRET, fehlende Stripe-Secrets) → Boot-Abbruch; Dev nur Warnung. Verpflichtende Stripe-Webhook-Signaturprüfung als pure HMAC-SHA256 (Schema `t=,v1=`, Toleranz, timing-safe) hinter `StripeWebhookGuard` + `POST /payments/webhook` (rawBody:true additiv). PII-Redaction (`redact`, immutabel) im Exception-Filter; GDPR `GET /account/export` (Auskunft) + `POST /account/delete` (Anonymisierung statt Hard-Delete → Geld-/Audit-Trail bleibt auditierbar, Constitution II). `AuditLog`-Tabelle + `AuditService` (PII-redacted) als Zugriffsprotokoll, wired in auth login/login_failed/register. Starke Passwort-Policy als DTO-`@IsStrongPassword`; pure RFC-6238-TOTP + env-gegateter `AdminMfaGuard` (No-op ohne `ADMIN_TOTP_SECRET`, bewusst kein voller Per-User-Enrollment). Frontend: purer Passwort-Stärke-Scorer + Live-Meter auf Registrierung, `maskEmail`, GDPR-`PrivacyPanel` (Export + zweistufige Löschung) + `/account`-Seite + Nav-Link. Prisma-Migration `security_hardening` (AuditLog + User.anonymizedAt), Seed um Audit-Demodaten erweitert + lauffähig. API 290 Tests, Web 178 Tests grün, Per-Path-80%-Gates auf 13 Backend- + 5 Frontend-Cores, beide Builds grün. Live-Smoke verifiziert: Header gesetzt, Rate-Limit greift (→429), Webhook ohne Signatur →400, schwaches Passwort →400, GDPR Export/Delete ok. Bewusste Out-of-Scope (ehrlich): kein verteiltes Rate-Limiting/Redis, kein echtes CAPTCHA, kein KMS/Vault, kein Per-User-MFA-Enrollment, kein CI-SAST-Lauf — stattdessen Security-Release-Checkliste in `specs/007-security-hardening/quickstart.md`. Naechster (Phase 2): E7 (Observability & Funnel-Analytics, Branch 008 von 007-security-hardening).
- 2026-06-28: **E7 FERTIG** (PR #7, Phase 2 — letzter Epic der Kette). Observability & Funnel-Analytics als eigenes `observability`-Modul, bewusst OHNE neue externe Infra (in-memory Ring-Buffer + Postgres, dependency-frei — Linie der E5-/E6-Stores). Strukturiertes PII-armes Logging mit Korrelations-ID: Request-ID-Middleware (übernimmt/erzeugt `x-request-id`, spiegelt in Response, pure `request-id.ts`) + globaler `MetricsInterceptor` via `APP_INTERCEPTOR` (misst Latenz/Status/Route je Request, redacted Logzeile, verändert Response nie — bricht keine Unit-Tests, analog E6-Guard). System-Metrics als purer Aggregator (Fehlerrate, p50/p95-Latenz, Payment-Erfolgs-/Fehlerrate) — erfasst den Donation-Pfad von außen, kein Eingriff in den geprüften E2-Money-Code. Datenschutzkonforme Produkt-/Funnel-Analytics: consent-gegateter, boundary-validierter Ingest (`POST /analytics/events`, whitelist `@IsIn`), `AnalyticsEvent` mit anonymer `visitorId` statt IP + redacted Metadaten; pure Funnel-Aggregation (Donation + Onboarding, Conversion/Drop-off). Payment-/Webhook-Monitoring als Read-Aggregation über `Donation` + Webhook-Fehler aus dem Ring-Buffer -> purer Alert-Kern (Card-Decline-Welle/hängende Pledges/Webhook-Fehler mit Schweregrad). Uptime: öffentlicher `GET /health` (Liveness + DB-Probe) für externes Synthetic-Monitoring + purer Multi-Window-Multi-Burn-Rate-SLO-Kern (Google SRE: fast 1h/5m -> page, slow 6h/30m -> ticket). Frontend: `AnalyticsService` (consent-gegated, fire-and-forget, anonyme visitorId), Consent-Banner in App, Funnel-Events in Gallery/Campaign/Donate-Card; Operator-Dashboard `/admin/observability` (Funnel-Chart, System-Metrics + Payment-Alerts + Health, SLO/Burn-Rate-Panel). Prisma-Migration `observability_analytics` (`AnalyticsEvent`), Seed um 280 Funnel-Demo-Events erweitert + lauffähig. API 364 Tests, Web 222 Tests grün, Per-Path-80%-Gates auf 13 Backend- + 11 Frontend-Cores, beide Builds grün. Live-Smoke verifizierte einen echten DI-Bug (MetricsStore numerischer Ctor-Arg -> via Factory provided) und danach: `x-request-id`-Korrelation, Ingest 201 + Boundary 400, Admin-Guard 401, Admin-Dashboard liefert Funnel/Metrics/SLO/Alerts, globaler Interceptor erfasst echte Requests. Bewusste Out-of-Scope (ehrlich): keine externe Observability-Infra (Prometheus/Grafana/OTel/Sentry), Stores per-Instanz, SLO rechnet auf dem Sample-Fenster (kein Zeitreihen-Backend), Alerts berechnet+geloggt statt versendet (kein SMTP/PagerDuty), `GET /health` ist der Probe-Endpunkt (externes Polling out of scope). Kette E1->E5 + Phase-2 E6/E7 vollständig fertig.
- 2026-06-29: **E8 FERTIG** (PR #8, Welle 2 / Welle A). School-Self-Serve-Portal & Partner-Onboarding: das bestehende `schools`-Modul vom dünnen CRUD zum vollen Self-Serve-Portal ausgebaut, bewusst OHNE neue externe Infra — e-Signatur und Registrar liegen hinter austauschbaren Interfaces (`EsignatureProvider`/`RegistrarProvider`, Mock by default, Factory mit DocuSign→Mock-Fallback; KEIN echter Call), Linie der `PaymentProvider`-Naht. Neue Rolle `SCHOOL_ADMIN`, an genau eine Schule gebunden (`SchoolAdmin`-Join); jede Portal-Route ist auf die eigene Schule skopiert, IBAN in Antworten maskiert. Studierende-Verifizierung: purer CSV-Parser (`admission-import`, Header email/name/program/admissionRef, Dedupe, Per-Zeilen-Fehler) → idempotenter Upsert auf `(schoolId, admissionRef)`; Verifizieren konsultiert die gemockte Registrar-Naht (Sentinel `…-UNKNOWN` → `409`), emittiert `student.reported`. Auszahlungsdaten Self-Service + Mock-e-Signatur → Aktivierung setzt `payoutVerified` (das bestehende Trust-Gate, Constitution II — Geld weiterhin nur an die Schule). Kampagnen-Genehmigung schul-skopiert über DIESELBE Verifizierungs-Transition wie der Admin-Pfad (Zulassung VERIFIED → Kampagne LIVE), gegated auf ACTIVE + payout-verifiziert, emittiert `campaign.approved`. Hosted Onboarding (à la Stripe Connect): purer Einmal-Token (`onboarding-token`, nur SHA-256-Hash gespeichert, timing-safe, used/expired/mismatch), `complete` aktiviert transaktional + verbraucht den Token. Echtzeit-Dashboard: purer `payout-status` + `school-dashboard` (Totals, Per-Studierenden-Status, Spender-Geografie über neues `Donation.donorCountry`). School-Webhooks als Stub-Emitter (persistiert + strukturiert geloggt, schluckt Fehler → bricht nie die Geschäftsoperation); `payout.sent` hängt fire-and-forget hinter dem geprüften Disburse-Block (Geld-Pfad unangetastet). Prisma-Migration `school_portal` (SchoolAdmin, AdmissionRecord, SchoolOnboardingToken, SchoolWebhookEvent, School-Onboarding/Payout-Felder, Donation.donorCountry, Enum SchoolOnboardingStatus, Role.SCHOOL_ADMIN), Seed um `schooladmin@bursa.test` (ESMT, ACTIVE) + Admissions + Webhook-Log + hosted RSM-Link erweitert + idempotent. Frontend: gebrandeter `/school`-Shell (Schulname + Bursa, Onboarding-Banner + Tabs), Dashboard/Students/Payout/Campaigns/Webhooks-Panels, öffentliche `/school/onboarding/:token`-Seite; pure Helfer (onboarding-progress/admission-status/school-dashboard-format) gegated. API 440 Tests, Web 238 Tests grün, Per-Path-80%-Gates auf 14 Backend- + 3 Frontend-Cores, beide Builds grün, Seed läuft. Bewusste Out-of-Scope (ehrlich): kein echter DocuSign-/Registrar-Call, keine echte Webhook-Auslieferung (nur Log), keine Multi-Tenant-Subdomains, kein Schul-Admin-Self-Signup, kein Rate-Limit auf dem hosten Endpunkt (256-bit-Token-Entropie), kein Spender-Geo-Capture im Donate-Flow. Erstes Epic der Welle 2 (Roadmap v2).
- 2026-06-29: **E9 FERTIG** (PR #9, Welle 2 / Welle A). Trust-and-Safety Operations Console als eigenes `trust-safety`-Modul, bewusst OHNE neue externe Infra und OHNE echtes ML/Stripe Radar — alle Scores/Entscheidungen sind deterministische, pure Heuristik-Regelfunktionen (Linie der E5-/E6-/E7-Stores). Der geprüfte Geld-Pfad bleibt unangetastet: ein Freeze setzt nur Status-Flags (`Campaign.frozen`/`User.frozen`), ein Auto-Refund-Angebot ist nur ein Status (kein echter Refund), `PaymentProvider` unverändert, Geld weiterhin nur an die Schule (Constitution III). **Wiederverwendung statt Neubau:** E6-`AuditService` trägt jede Moderations-/Freeze-/Chargeback-Entscheidung (wer/wann/Aktion/Grund/Resultat; CSV-Export aus `AuditService.list`), E6-`StripeWebhookGuard` schützt den Chargeback-Webhook (HMAC über Raw-Body, timing-safe), E7-`AnalyticsService` erhält `trust.fraud_signal`/`trust.chargeback`/`trust.flag`-Events fire-and-forget — kein zweites Audit-/Metriken-System. Campaign Moderation Queue: pure `moderation-rules` + `ofac-keyword-matcher` (statische Sanktionsliste, verdächtige Keywords, Duplicate-Detection per Token-Jaccard) → Auto-Flag mit Score/Level/Reasons; Operator approve/reject/escalate (Pflicht-Grund), Reject friert ein. Fraud-Scoring pro Transaktion: pure `card-testing` (schnelle/fehlgeschlagene Versuche) + `donor-risk` (Geo/Size/Velocity/Card-Type, Auto-Flag >5k EUR) + `velocity-tracker` (>5/1h) aggregiert via `fraud-score`; persistiert `FraudSignal`, setzt Donor-`riskScore`, Donor-Auto-Freeze bei 2+ Failed + Chargeback (pure `auto-freeze`). Dispute-/Chargeback-Management: Queue gespeist von gemockten Stripe-Chargeback-Webhook-Events (idempotent über `providerEventId`), Evidence-Stub, Auto-Refund-Angebot für niedrige Disputes (pure `chargeback-rules`), Kampagne auto-frozen bei 3+ Chargebacks. Community-Flagging: öffentlicher Reporter-Button (`POST /campaigns/:id/flags`, OptionalJwt + E6-Rate-Limit) auf der Kampagnen-Seite. Dashboard & Reports: pure `dashboard-aggregation` (Fraud-Trends/Chargeback-Rate/Backlog) + `risk-heat-map` (Geo) im read-only Muster von `PaymentMonitorService`; Audit-CSV-Export. Prisma-Migration `trust_safety` (Enums ModerationStatus/RiskLevel/ChargebackStatus/FlagReason/FlagStatus; ModerationCase/FraudSignal/Chargeback/CampaignFlag; Freeze-Felder auf Campaign, Freeze-+Risk-Felder auf User; Audit-Trail nutzt die bestehende E6-`AuditLog`), Seed um auto-geflaggte Kampagne, Moderations-Fälle, Demo-Chargebacks (eine Kampagne auto-frozen), Fraud-Signale, Donor-Risk-Score und Community-Flag erweitert + idempotent. Frontend: Operator-Konsole `/admin/trust-safety` (Tabs Dashboard/Moderation/Chargebacks, KPI-Tiles, Fraud-Trend, Heat-Map, approve/reject/escalate + Evidence/Refund), pure Helfer (risk-format/moderation-format/chargeback-format) gegated; Community-Flag-Button an der Kampagne. API 548 Tests, Web 252 Tests grün, Per-Path-80%-Gates auf 15 Backend- + 3 Frontend-Cores, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein echtes ML-Modell/kein echtes Stripe Radar (deterministische Heuristik), keine echte OFAC-/Sanctions-API/kein Live-Feed (statische Liste), keine echte Stripe-Dispute-Integration (gemockte, signaturgeprüfte Webhooks), kein echter Refund-Vollzug (Angebot=Status), kein verteiltes/Echtzeit-Fraud-System (single-instance/in-memory), kein CAPTCHA auf dem Flag-Endpunkt (E6-Rate-Limit als Schutz). Zweites Epic der Welle 2 (Roadmap v2) nach E8.
- 2026-06-29: **E10 FERTIG** (PR #10, Welle 2 / Welle A). AI Fundraising Coach als eigenes `ai`-Modul, bewusst OHNE Live-LLM-Aufruf im Default-Lauf und OHNE neue Dependency — die LLM-Anbindung sitzt hinter einer austauschbaren `TextGenerationProvider`-Naht (Symbol-DI-Token, exakt das `PaymentProvider`-Muster, Constitution III): `MockTextGenerationProvider` (Default, deterministisch, KEINE Netzaufrufe, läuft ohne Key) + echtes `ClaudeTextGenerationProvider`-Skeleton (`fetch` → Anthropic Messages API, lazy, gated, Modell-Default `claude-sonnet-4-6` / Alternative `claude-opus-4-8`), gewählt durch die pure `createTextGenerationProvider`-Factory (`AI_PROVIDER=mock|claude` + `ANTHROPIC_API_KEY`, Fallback auf Mock). Der Coach assistiert dem E3-Wizard, er ersetzt ihn NICHT: Story-Draft mappt 1:1 auf die bestehenden Story-Teile (`storyBackground/-Challenge/-Vision`), Titel-Übernahme nur auf bewusste Nutzer-Aktion, manueller Tipp-Flow + `composeStory`/`isStoryReady` unverändert. **Pure, getestete Kerne (je Per-Path-80%-Gate):** `prompt-builder` (System-/User-Prompts für Titel/Story/Share je Kanal+Locale, Brand-Regeln zentral), `tone-postprocessor` (Anti-Slop-Phrasen/Intensifier raus, Em-Dash→Bindestrich, bei `locale=de` ECHTE Umlaute über konservative Wortliste — nie blind), `variant-ranking` (trim/dedupe/leere-raus, deterministisches Längen-Fenster-Ranking + genau eine Empfehlung, reproduzierbar für 1-Klick-Refresh/A-B), `token-budget` (immutable Verbrauchs-/Restbudget-Rechnung). Service dünn: Budget-Guard → Prompt (pur) → Provider → Tone+Rank (pur) → Budget-Update + `AiGeneration`-Log; erschöpftes Budget → `429 BUDGET_EXCEEDED` OHNE Provider-Aufruf (keine stillen Kosten). Endpunkte STUDENT-only (`GET /ai/budget`, `POST /ai/title|story|share`), Boundary-Validation per class-validator-DTO, globale `{success,data}`-Envelope. `ANTHROPIC_API_KEY` bleibt serverseitig. Prisma-Migration `ai_coach` (Enum `AiGenerationKind`; Tabellen `AiTokenBudget`/`AiGeneration`; `User.aiTokenBudget`/`User.aiGenerations`-Relationen; rein additiv), Seed um sichtbares Demo-Token-Budget + Generierungs-Log für `amara@bursa.test` erweitert + idempotent. Frontend: assistive `AiCoachPanelComponent` im Wizard-Story-Schritt (Titel/Story/Share generieren, Sprache EN/DE, Refresh, "Use"-Übernahme, Restbudget-Meter), pure Helfer (`ai-coach.helpers`: channel-/kind-Labels, Restbudget-Format, Varianten-Preview, Empfehlung) gegated; `ApiService`-Coach-Methoden via `unwrap(http.post(Envelope))`; E3-Wizard-Spec um die Apply-Handler + School-Name-Computed erweitert (E3 bleibt grün). API 620 Tests, Web 270 Tests grün, Per-Path-80%-Gates auf 7 Backend- (4 pure Kerne + Mock-Provider + Factory + Service) + 1 Frontend-Core, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein Live-LLM-Aufruf in Tests/Default (Mock), kein Fine-Tuning/kein eigenes Modell/kein RAG (Brand-Stimme über Prompt + deterministischen Post-Processor), kein echtes Token-/Billing-System (einfacher persistierter Pro-Nutzer-Zähler), keine i18n über DE/EN hinaus, kein Streaming/keine clientseitigen LLM-Calls. Drittes Epic der Welle 2 (Roadmap v2) nach E8/E9.
- 2026-06-27: **E2 FERTIG** (PR #2). All-or-Nothing Pledge/Capture-Engine: PaymentProvider um savePledge/captureOnGoalReached/payoutToSchool erweitert, Mock (Default) + echter StripePaymentProvider hinter PAYMENT_PROVIDER-Flag (lazy SDK, Fallback Mock). Kartenspende = PLEDGED (kein Charge), Capture aller Pledges bei Zielerreichung -> CAPTURED + Beleg, Kampagne FUNDED. Prisma-Migration payments_allornothing (DonationStatus +PLEDGED/CAPTURED/EXPIRED, +pledgeRef/+capturedAt). Frontend goal-math + campaign-progress (Restsumme, 80/90%-Meilenstein, Deadline, AoN-Hinweis). API 50 Tests, Web 46 Tests gruen, Per-Path-80%-Gates, beide Builds gruen, Seed laeuft. Naechster: E3 (Branch 004 von 003-payments-allornothing).
- 2026-06-30: **E11 FERTIG** (PR #11, Welle 2). Automated KYC & Verification Pipeline als eigenes `kyc`-Modul, bewusst OHNE echten Liveness-/OCR-/AML-Call im Default-Lauf und OHNE neue Dependency — Identität und AML sitzen hinter zwei austauschbaren Symbol-DI-Nähten (exakt das `PaymentProvider`-Muster, Constitution III): `IdentityVerificationProvider` (Liveness + Dokument-OCR) mit `MockIdentityVerificationProvider` (Default, deterministisch, KEINE Netzaufrufe) + echtem `PersonaIdentityProvider`-Skeleton (`fetch`, gated über `KYC_PROVIDER=mock|persona` + `PERSONA_API_KEY`, Fallback Mock), und `AmlScreeningProvider` mit `MockAmlScreeningProvider` (Default) + echtem `SumsubAmlProvider`-Skeleton (`fetch`, gated über `AML_PROVIDER=mock|sumsub` + `SUMSUB_API_KEY`, Fallback Mock); beide Skeletons kompilieren, werden im Test nie ausgeführt. **Wiederverwendung statt Neubau:** jede Verifikations-Entscheidung wird über das E6-`AuditService`/`AuditLog` protokolliert (`kyc.*`-Actions, PII-redigiert) — kein zweites Audit-System; der Dokument-Schritt gleicht den OCR-Namen gegen den E8-`AdmissionRecord` ab und bestätigt zusätzlich über den E8-`RegistrarProvider` (`lookupAdmission`) — kein zweiter Registrar-Seam; die Sanktionsliste baut auf der E9-`SANCTIONED_COUNTRIES`-Liste auf (Single Source) und das `RiskLevel`-Band wird wiederverwendet. Der geprüfte Geld-Pfad bleibt unangetastet, Geld weiterhin nur an die Schule (Constitution II). **Pure, getestete Kerne (je Per-Path-80%-Gate):** `liveness-result` (Confidence-Schwelle → pass/fail), `name-match` (Levenshtein-Ähnlichkeit → 0-100 + matched, toleriert Tippfehler, schickt echten Mismatch in die Review), `sanctioned-country` (OFAC/Country-Klassifikation CLEAR/ELEVATED/SANCTIONED), `aml-decision` (Schwelle >5.000 EUR + Land + Provider-Treffer → CLEAR/HIT/BLOCKED), `risk-score` (geo + income + accreditation → 0-100 + Band, Muster `aggregateFraudScore`), `verification-state` (State-Machine + Review-Queue-Ableitung — jede Ausnahme landet in MANUAL_REVIEW, nie stiller Erfolg), `case-view` (DB-Row → View-Mapper). Services dünn: `KycService` orchestriert Liveness → Dokument (+Registrar) → Risk bzw. Sponsor-AML, schreibt DB, auditiert; `KycReviewService` führt die Manual-Review-Queue (Operator APPROVE/REJECT, Pflicht-Note) + Dashboard. Endpunkte rollenscharf: STUDENT (`POST /kyc/cases`, `/liveness`, `/document`, `GET /kyc/cases/me`), SPONSOR (`POST /kyc/aml/screen` — sanktioniertes Land → 422 AML_BLOCKED), ADMIN (`GET /kyc/review/queue|dashboard|:id`, `POST /kyc/review/:id/decide`), Boundary-Validation per class-validator-DTO, globale `{success,data}`-Envelope. Deterministische Mock-Sentinels (`-FAIL` Liveness, `-MISMATCH` OCR, Land RU/IR→BLOCKED, NG/PK→HIT) machen alle Pfade demobar. Prisma-Migration `kyc_verification` (Enums VerificationSubject/VerificationCaseStatus/ReviewQueueStatus/AmlDecision; Tabellen VerificationCase/LivenessResult/DocumentVerification/AmlScreening; Relationen an User + AdmissionRecord; rein additiv), Seed um synthetische Fälle (VERIFIED Student, MANUAL_REVIEW OCR-Mismatch, Sponsor-AML-HIT) + zwei E6-Audit-Einträge erweitert + idempotent. Frontend: Student-Verifikations-Flow (`KycVerificationComponent` Liveness/Dokument/Status im Studi-Dashboard, pure `kyc-status`-Helfer), Operator-Konsole `/admin/kyc` (`KycReviewPage` + `KycReviewQueueComponent` mit Approve/Reject, pure `kyc-review-format`), Sponsor-AML-Status (`AmlStatusComponent` + pure `aml-status`); `ApiService`-KYC-Methoden via `unwrap(http.post(Envelope))`. API 732 Tests, Web 307 Tests grün, Per-Path-80%-Gates auf 13 Backend- (7 pure Kerne + 2 Mock-Provider + 2 Factories + 2 Services) + 6 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein echter Liveness-/OCR-/AML-Call in Tests/Default (Mocks), kein Live-Sanktions-Feed (statische Liste), kein echtes Biometrie-/OCR-Modell (Mock-Payload + Levenshtein), kein Datei-Upload-/Storage-Subsystem (strukturierte Mock-Tokens), keine echte Income-Verification (deklarierter Risk-Input), kein Multi-Instanz-/Worker-Setup (synchron, single-instance), keine automatische Kampagnen-Live-Schaltung aus diesem Epic. Viertes Epic der Welle 2 (Roadmap v2) nach E8/E9/E10.
- 2026-06-30: **E12 FERTIG** (PR offen, Welle 2). Payout-Reconciliation & Transparenz-Layer in zwei neuen Modulen, bewusst OHNE echten Bank-Feed-Call im Default-Lauf und OHNE neue Dependency. Kern ist ein **wiederverwendbares append-only Transaction-Ledger** (`ledger`-Modul), das E14 (CSRD-Audit-Trail) mitnutzen kann: das `LedgerEntry`-Modell ist literally append-only (kein `updatedAt`, kein Soft-Delete), der `LedgerService` bietet AUSSCHLIESSLICH `append()` + `read()` (KEINE update-/delete-Methode existiert — die durchgesetzte Immutabilitäts-Invariante, Constitution IV), und jeder Eintrag ist über eine SHA-256-Hash-Chain (`prevHash` → `entryHash`, monoton je Schule) manipulationssicher verkettet; `LedgerModule` exportiert den Service, damit E14 nur lesend darauf aufsetzt. Der Bank-Feed sitzt hinter einer austauschbaren Symbol-DI-Naht (exakt das `PaymentProvider`-Muster, Constitution III): `BankFeedProvider` mit `MockBankFeedProvider` (Default, deterministisch, KEINE Netzaufrufe — leitet Bank-Transaktionen aus den System-Payouts ab, Sentinels `-STALE`/`-DISCREPANCY` + Orphan-Tx) + echtem `PlaidBankFeedProvider`-Skeleton (`fetch` → Plaid `/transactions/get`, gated über `BANK_FEED_PROVIDER=mock|plaid` + `PLAID_SECRET`, Fallback Mock, nie im Test ausgeführt), gewählt durch die pure `createBankFeedProvider`-Factory. **Wiederverwendung statt Neubau:** die Reconciliation liest die bestehenden E2-`Payout`-/`Donation`-Datensätze (der geprüfte Geld-Pfad wird NUR gelesen, nie mutiert — Geld weiterhin nur an die Schule, Constitution II); der dependency-freie E5-`buildSimplePdf`-Writer + das E5-CSV-`cell()`-Escaping werden für alle Exporte wiederverwendet (keine neue Export-Infra); das E8-Schul-Dashboard wird um eine Reconciliation-Tab ERWEITERT (nicht geforkt), über `SchoolPortalService.resolveSchoolId` schul-skopiert. **Pure, getestete Kerne (je Per-Path-80%-Gate):** `ledger-hash` (kanonische Serialisierung + sha256 + `verifyChain`), `ledger-entry` (Builder), `ledger-view` (DB-Row → View + Integritäts-Faltung), `reconciliation-matcher` (Payouts ⟷ Bank-Tx → MATCHED/PENDING/UNMATCHED + Orphans, Match per Referenz ODER Betrag+72h-Fenster), `discrepancy-detector` (Betrags-Abweichung → DISCREPANCY), `stale-payout-alert` (48h-Entscheidung), `tax-report` (US 1099 / EU SEPA je `School.country`, IBAN maskiert), `double-entry` (Debit/Credit + GL-Codes 5000/1000, balanciert), `transparency-aggregator` (PII-freie Aggregate), `reconciliation-export` (CSV/PDF-Zeilen), `reconciliation-view` (Komposition → `ReconciliationView`). Service dünn: liest Payouts + Bank-Feed, läuft durch die puren Cores, persistiert Bank-Transaktionen idempotent (`(provider, externalId)`) und einen `Reconciliation`-Verlauf. Endpunkte: SCHOOL_ADMIN (`GET /school/reconciliation`, `/payouts`, `/ledger` + vier Datei-Exporte `export.csv|export.pdf|tax-report.csv|accounting.csv` via `@Res()`), öffentlich ohne Auth (`GET /transparency/schools/:id` — nur Aggregate, keine PII). Prisma-Migration `payout_reconciliation` (Enums LedgerEntryType/ReconciliationStatus; Tabellen LedgerEntry [append-only, unique `(schoolId,sequence)`]/BankTransaction [unique `(provider,externalId)`]/Reconciliation; Rück-Relationen an School/User/Payout; rein additiv), Seed um Ledger-Einträge mit gültiger Hash-Chain für alle gecapturten Spenden + Payouts + Disbursements, eine gematchte Bank-Tx, eine `-STALE`-Auszahlung (>48h, kein Bank-Match → 48h-Alert) und eine Orphan-Bank-Tx erweitert + idempotent. Frontend: `SchoolReconciliationComponent` als neue Reconciliation-Tab im `/school`-Portal (Summary-Tiles, Payout-Tabelle mit Status-Chips + Discrepancy-Flags, 48h-Alert-Box, vier Export-Buttons, Ledger-Integritäts-Anzeige), pure `reconciliation-format`-Helfer gegated; öffentliche `/transparency/:schoolId`-Seite (`TransparencyPage` + pure `transparency-format`: Stat-Tiles, Paid-out-Balken, Spender-Geografie); `ApiService` + `models.ts` um die Reconciliation-/Ledger-/Transparency-Calls + Typen ergänzt. API 839 Tests, Web 331 Tests grün, Per-Path-80%-Gates auf 15 Backend- (12 pure Kerne + Mock-Provider + Factory + Service) + 4 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein echter Bank-Feed-/Plaid-Link-Call in Tests/Default (Mock), kein echter Push zu QuickBooks/Wave/NetSuite (Datei-Export zum Import), keine rechtlich zertifizierten Tax-Reports (illustratives 1099-/SEPA-Format), kein echtes Bankkonto-Linking/Plaid-OAuth, kein Multi-Instanz-/Scheduler-/Worker-Setup (Reconciliation synchron auf Abruf, 48h-Alert bei Abruf berechnet), keine krypto-starke Manipulationssicherheit (SHA-256-Integritäts-Chain, keine Signatur/Notarisierung/Blockchain). Fünftes Epic der Welle 2 (Roadmap v2) nach E8/E9/E10/E11; liefert das append-only Ledger-Primitive für E14.
- 2026-06-30: **E13 FERTIG** (PR offen, Welle 2). Employer Matching Auto-Detection als eigenes `matching`-Modul, bewusst OHNE Umbau des geprüften E2-Geld-Pfads und OHNE echte Double-the-Donation-API. **Provider-Abstraktion exakt wie `PaymentProvider`:** die Arbeitgeber-DB liegt hinter `EmployerMatchProvider` (Symbol-Token, `lookupByDomain`); Default = deterministischer `MockEmployerMatchProvider` (statische ~30-Firmen-DB, in allen Tests genutzt, kein Netz), echtes `DoubleTheDonationProvider`-`fetch`-Skeleton env-gated (`EMPLOYER_MATCH_PROVIDER=mock|dtd` + `DTD_API_KEY`, Default `mock`, nie in Tests), pure Factory `createEmployerMatchProvider`/`shouldUseDtd` mit Mock-Fallback. **Reine Cores (TDD, Per-Path-80%):** `email-domain` (Domain-Extractor), `employer-match-lookup` (Eligibility: aktiv + minDonation), `match-amount` (Ratio×Spende, hart gedeckelt durch verbleibendes Jahres-Limit, capped-Flag), `claim-status` (State-Machine + Display), `match-labels` (EN/DE/FR/ES-Resolver, Arbeitgeber-Namen mit echten Akzenten), `match-view` (Offer/Claim/Balance-View-Mapper + Apply-URL-Substitution). **Wiederverwendung statt Neubau:** der geclaimte Match wird als ganz normale CORPORATE-`Donation` (SEPA/SUCCEEDED, `donorName=employerName`) auf dieselbe Kampagne gebucht und mit der bestehenden E2-`splitContribution`-Over-Funding-Kappung gedeckelt (kein echter Charge, Geld weiterhin nur an die Schule, Constitution II/III); der detektierte Arbeitgeber + die Jahres-Match-Balance leben am E4-`User` (`employerName`/`employerDomain`/`matchYear`/`matchUsedCents`, impliziter Jahres-Reset); die Claim-Bestätigungs-PDF nutzt die E5-`buildSimplePdf`-Util (keine neue Infra). Endpunkte: `POST /matching/detect|offer|claim` (OptionalJwt, idempotent je Spende über `MatchClaim.donationId @unique`), `GET /matching/me/balance` + `…/claims/:id/document` (DONOR-skopiert). Prisma-Migration `employer_matching` (Enums MatchClaimStatus/EmployerIntegrationLevel, Modelle EmployerMatchProgram/MatchClaim, User-Match-Felder + Donation-Rück-Relationen), Seed um ~30 Programme + `donor@bursa.test`→SAP mit Demo-Claim erweitert + idempotent. Frontend: Match-Offer-Callout im Checkout (Arbeits-E-Mail → Angebot → 1-Tap-Claim als Link oder PDF) in `donate-card`, Match-Balance-Counter ("X € Match noch verfügbar dieses Jahr") + Claim-History im `/donor`-Account; pure Helfer (`match-format`/`employer-label`) + drei Komponenten gegated. API 922 Tests, Web 365 Tests grün, Per-Path-80%-Gates auf 8 Backend- + 5 Frontend-Cores, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): keine echte Double-the-Donation-API (deterministischer Mock), keine automatische Antrags-Einreichung (Link/PDF, Spender reicht selbst ein), kein echtes Corporate-Settlement (Match = Prototyp-Zusage als CORPORATE-Spende), keine Arbeits-E-Mail-Verifikation (Domain aus Eingabe), kein Jahres-Reset-Cron (impliziter Reset beim ersten Claim im neuen Jahr), keine Kopplung an das tiefe Corporate-Matching E21 (ADP/Workday). Sechstes Epic der Welle 2 (Roadmap v2) nach E8/E9/E10/E11/E12.
- 2026-06-30: **E14 FERTIG** (PR offen, Welle 2). ESG/CSR Audit-Trail & CSRD-Compliance-Reporting als eigenes `esg`-Modul — eine reine **Read-/Aggregate-Schicht über dem E12-Ledger**, bewusst OHNE zweites Transaction-Log und OHNE Mutation des append-only Ledgers (Constitution II/IV). **Wiederverwendung statt Neubau:** das E12-`LedgerService` wird ausschliesslich lesend genutzt (`listAllForReporting` neu hinzugefügt, plus das exportierte pure `buildLedgerView` für die Chain-Integritäts-Prüfung im Auditor-Portal — kein Append/Update/Delete in E14); Compliance-Tags hängen ADDITIV über eine separate `EsgTag`-Tabelle (`@unique ledgerEntryId`) am Eintrag, der Ledger-Eintrag bleibt byte-identisch und damit hash-verifizierbar; die Report-Exporte nutzen die dependency-freie E5-`buildSimplePdf`-Util + das E5-CSV-`cell()`-Escaping (keine neue Export-Infra); Tag-Setzen/Report-Generierung/Grant-Erstellung werden über die E6-`AuditService` protokolliert (PII-redacted, fail-soft), ESG-/Report-Events über die E7-`AnalyticsService` fire-and-forget emittiert; der zeitlich begrenzte Auditor-Token spiegelt exakt das E8-`onboarding-token`-Muster (32 Byte Zufall, nur SHA-256-Hash persistiert, timing-safe Validierung, injizierbare Zeit/Random). **Reine, getestete Cores (je Per-Path-80%-Gate):** `esg-category` (Enum-Validierung + Verteilung), `age-band` (Geburtsjahr → Altersband), `diversity-aggregator` (Gender/Land/Altersband/First-Gen-Verteilungen + female share über nur erfasste Werte), `data-quality` (captured/total → Completeness-Score je Feld + "collect more"-Flag), `esg-aggregate` (neutrales `EsgAggregate` aus Ledger+Profilen+Tags), `esg-standard-mapper` (illustratives GRI-2024-/CSRD-ESRS-/SASB-/UN-SDG-Mapping), `audit-annotation` (Fussnoten auf Quell-`entryHash` + CSV/PDF-Komposition via E5), `esg-trend` (Year-over-Year EUR/Scholar/Diversity + Delta), `auditor-access-token` (create/validate, TTL default 48h). Service dünn darüber. Endpunkte: ADMIN (`POST /admin/esg/tags`, `PUT …/diversity/:id`, `GET …/report?standard=&year=`, `POST/GET …/reports`, `GET …/reports/:id/export.csv|.pdf` via `@Res()`, `GET …/data-quality`, `GET …/trend`, `POST/GET …/auditor-grants`, `POST …/:id/revoke`) + öffentlich token-gegatet READ-ONLY (`GET /audit-portal/:token` → Trail-Auszug + Integrität; abgelaufen/unbekannt → 401, widerrufen → 403, nie Schreibzugriff). Prisma-Migration `csrd_reporting` (Enums EsgCategory/ReportStandard/Gender; Modelle EsgTag/EsgReport/AuditorAccessGrant; `StudentProfile` += optionale `gender`/`birthYear`/`firstGen` [alle nullable → E8-Onboarding/E11-KYC unberührt]; `LedgerEntry` += `esgTag`-Back-Relation OHNE Feld-Change → Hash-Chain unverändert), Seed um synthetische Scholar-Diversity (11 Profile, bewusst lückenhaft für realistischen Data-Quality-Score), 3 ESG-Tags auf bestehende Ledger-Einträge, einen erzeugten GRI-Report und einen Demo-Auditor-Grant erweitert + idempotent (E12-Ledger-Einträge NICHT mutiert). Frontend: ADMIN-Seite `/admin/csrd` (Report-Builder mit Standard-Picker + Generate + CSV/PDF-Export, Data-Quality-Panel, Year-over-Year-Trend-Tabelle, Auditor-Access-Panel mit Grant-Erstellung/-Widerruf + Einmal-Portal-Link), pure Helfer (`csrd-format`/`data-quality-format`/`trend-format`/`auditor-grant-format`) + vier Komponenten gegated, Nav-Link "ESG / CSRD" im Admin-Header. API 999 Tests, Web 397 Tests grün, Per-Path-80%-Gates auf 10 Backend- (9 pure Cores + Service) + 8 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein zertifiziertes Disclosure (GRI/CSRD/SASB/SDG-Mapping illustrativ, kein XBRL/ESEF), keine echte externe Assurance-Integration (kein Prüfer-Workflow/Wirtschaftsprüfer-Signatur), Diversity-Daten optional/synthetisch (kein HRIS-Import), kein zweites Ledger/keine Ledger-Mutation, Single-Instance (kein verteiltes Sharing/S3-Bucket/Rate-Limit-Redis), kein Volunteering-Tracking. Siebtes und letztes Epic der Welle 2 (Roadmap v2) nach E8/E9/E10/E11/E12/E13.
- 2026-06-30: **E16 FERTIG** (PR offen, Welle 2). Spender-Portfolio & Giving-Streaks. **Kernidee: die Gamification-Primitive EINMAL als generische, donation-freie reine Bausteine bauen** (`apps/api/src/gamification/`), damit E15 (Referral/Advocate) und E18 (Gruppen) sie ohne Umbau wiederverwenden — sie kennen kein Donation-Modell, nur Zeitstempel/Zählwerte/Scores: `streak.util` (`computeMonthlyStreak(timestamps, referenceDate)` → aktuelle/längste Streak in Kalendermonaten + Grace-Monat, deterministisch via injiziertem Referenzdatum, NIE `Date.now()`), `badge.util` (`resolveTier(value, thresholds)` + `monthlyGivingBadge(streakMonths)` → Bronze/Silber/Gold bei 3/6/12 + nächster Tier + Abstand), `leaderboard.util` (`rankLeaderboard(entries)` → Score desc, stabiler Tie-Break id asc, mit Rang), `cumulative.util` (`aggregateContributions(items)` → Summe/Count/distinct Targets/Impact-pro-Target/erster+letzter Monat), `peer-comparison.util` (`comparePeers(yourValue, populationValues)` → motivierender, nie beschämender Aggregat-Vergleich). **Wiederverwendung statt Neubau:** das Portfolio liest dieselben E4-Donor-Donations (Status-Filter `PLEDGED|CAPTURED|SUCCEEDED` wie `DonorsService.COUNTED`), zeigt pro unterstützter Kampagne Foto/Name (StudentProfile) + Fortschritt (E3-`percentOf`) + Verified-State (E1-`verification.status`); die CSV/PDF-Exporte nutzen die dependency-freie E5-`buildSimplePdf`-Util + eine gespiegelte `cell()`-Escaping-Linie (kein Corporate-Import, keine neue Infra). Eigenes `portfolio`-Modul (`PortfolioService` baut `PortfolioView` aus Donations + Primitiven; Controller `donors/me/portfolio` + `…/export.csv|.pdf` via `@Res()`, alle `@Roles(DONOR)` wie E4). **KEINE Prisma-Migration** (kein neues Modell/Feld): Streaks/Badges/Stats werden on read abgeleitet, Badges sind rein motivierend (kein Award-Log, keine Rewards/Payouts — Geld bleibt schulgebunden, Constitution II); `migrate status` up to date, `diff --exit-code` "No difference detected". Seed: `donor@bursa.test` bekommt eine 7-Monats-Streak (→ SILVER) über 5 unterstützte Studierende (mehrmonatige CAPTURED-Donations, Tag-15-Anker gegen Monatsüberlauf), idempotent über `clearDatabase()`. Frontend (in den bestehenden E4-`/donor`-Account integriert): Streak-Banner (Streak-Text + Badge-Pill + nächster Meilenstein), kumulative Stats + motivierender Peer-Vergleich, mobile-first "My students"-Grid (Foto/Initial-Avatar/Progress-Bar/Verified-Badge/1-Tap "Donate again" → Kampagnen-Spendenpfad) + CSV/PDF-Export-Buttons; pure Helfer (`streak-format`/`portfolio-stats`) + drei Komponenten gegated. API 1042 Tests, Web 418 Tests grün, Per-Path-80%-Gates auf 7 Backend- (5 Gamification-Primitive + `portfolio-export.util` + `portfolio.service`) + 5 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein Echtzeit-Leaderboard/WebSockets (der Leaderboard-Ranker ist eine reine Funktion für E15/E18, kein Live-Board), keine persistierten Badge-Awards (on read berechnet), keine Rewards/Cashbacks an Badges/Streaks (Compliance), kein Streak-Cron, kein Privacy-Bruch im Peer-Vergleich (nur Aggregat-Durchschnitt, keine fremden Identitäten), kein Multi-Currency. Erster Consumer der wiederverwendbaren Gamification-Primitive (E15/E18 folgen).
- 2026-07-01: **E15 FERTIG** (PR offen, Welle 2). Referral- & Advocate-Engine als eigenes `referral`-Modul — EINE Engine mit zwei Gesichtern (Spender-Referrals im E4-Donor-Account + Studierenden-Advocates pro Kampagne), dieselbe Mechanik: eindeutiger Link, Attribution, Leaderboard, Feature-Rewards. **Erster echter Consumer der E16-Gamification-Primitive — KEIN zweites Leaderboard-/Badge-System gebaut:** `reward-tier.util.referralReward` ist ein dünner Wrapper über das E16-`badge.util.resolveTier` (Schwellen 3/5/10 → Shout-out/Recap/Recognition, reine Feature-/Recognition-Rewards, NIE Cash — Compliance), und `referral-leaderboard.util` mappt Referral-/Advocate-Counts auf das E16-`leaderboard.util.rankLeaderboard` (Score desc, stabiler id-asc-Tie-Break) für Advocate- UND anonymes opt-in-Referral-Board. **Wiederverwendung statt Neubau:** die eindeutigen Links spiegeln das E8-Einmal-Token-Muster (`referral-code.util`: 96-bit-Zufall, nur SHA-256-Hash gespeichert für Advocate-Invites, timing-safe Validierung, injizierbare `bytes()`-Quelle); der Referral-Link hängt am E4-Donor-Account (`donors/me/referral`, kein zweiter Account-Bereich); das Frontend-Sharing reust das E3-Share-Toolkit (`share-templates.ts` ruft E3-`buildShareLinks` für WhatsApp/Telegram-Deeplinks). Attribution ist **money-frei und last-touch**: gibt der Donate-Flow einen `referralCode` mit, ruft der Donations-Pfad nach erfolgreicher Spende `ReferralService.attributeDonation` fire-and-forget (schluckt jeden Fehler, bricht NIE den Geld-Pfad — Geld weiterhin nur an die Schule, Constitution II/III); Dedupe-Garantie über `ReferralAttribution.donationId @unique` (eine Spende zählt höchstens einmal), Self-Referral rein geblockt. **Pure, getestete Kerne (je Per-Path-80%-Gate):** `referral-code` (create/hash/validate, E8-Muster), `referral-attribution` (reine Decision: gültiger Code → gezählter Status → nicht self-referral → nicht schon zugeschrieben), `reward-tier` (Wrapper über E16-resolveTier + perks + both-win), `referral-leaderboard` (Wrapper über E16-rankLeaderboard, Advocate-Namen vs. anonyme "Supporter #N"), `share-template` (multi-channel pre-filled Email/WhatsApp/LinkedIn, kein echtes Mail-Versenden), `referral-stats` (invited/donated/active → Conversion-% + Viral-Coefficient + Label), plus `referral.service` (Prisma-I/O hinter den Primitiven, Advocate-Limit 15, gegated). Endpunkte: DONOR (`GET /donors/me/referral`, `POST …/leaderboard-opt-in`, `GET /referral/leaderboard`), STUDENT (`POST/GET /campaigns/:id/advocates`), auth-gated (`GET /campaigns/:id/advocate-leaderboard`); Boundary-Validation per class-validator-DTO, `{success,data}`-Envelope. Prisma-Migration `referral_engine` (Enums ReferralKind/AdvocateInviteStatus; Modelle ReferralLink [1 pro Donor, anzeigbarer `code` + `codeHash`]/AdvocateInvite [hash-only, ≤15/Kampagne]/ReferralAttribution [`donationId @unique` Dedupe, money-frei]; Rück-Relationen an User/Campaign/Donation; rein additiv), Seed um `donor@bursa.test`-Referral-Link (opt-in) + 3 geworbene Freunde [1 recurring=aktiv] + 3 Advocates auf Amaras Kampagne mit 10 geworbenen Spenden erweitert + idempotent. Frontend: Donor-Referral-Panel (Link, invited/donated/active-Tracking, Reward-Tier + perk + both-win, 1-Tap-Share, opt-in-Toggle) + Referral-CTA nach Spende, beide in den E4/E16-`/donor`-Account integriert; Advocate-Manager (einladen + Liste + Reward) im Studi-Dashboard; Advocate-Leaderboard auf der öffentlichen Kampagnen-Seite; pure Helfer (`referral-link`/`referral-stats-format`/`share-templates`) + vier Komponenten gegated. API 1106 Tests, Web 448 Tests grün, Per-Path-80%-Gates auf 7 Backend- (6 pure Kerne + `referral.service`) + 7 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): keine Cash-Rewards (nur Feature-/Recognition, Compliance), kein echtes Email-Versenden (Links + Templates, kein SMTP), Attribution last-touch (kein Multi-Touch), kein Echtzeit-Leaderboard/WebSockets (rankLeaderboard bei Aufruf), Single-Instance (DB-Unique als Dedupe, kein verteilter Lock), keine zweite Badge-/Leaderboard-Engine (E16-Primitive wiederverwendet), kein tiefes Self-Referral-/Abuse-Hardening über das simple Self-Referral-Block hinaus (E9/E11). Erster Consumer der E16-Gamification-Primitive über E16 selbst hinaus (E18 folgt). Achtes Epic der Welle 2 (Roadmap v2) nach E8–E14/E16.
- 2026-07-01: **E17 FERTIG** (PR offen, Welle 2 / Welle B — LETZTES Epic der Welle B). Multi-Channel Impact-Feed als eigenes `impact-feed`-Modul, sauber gegen E4 abgegrenzt: **der E4-E-Mail-Danke-Pfad (`NotificationsService.onDonation` → `thankYouNotification` → `EmailLogger` → `Notification`-Modell + `/notifications`-Feed) bleibt KOMPLETT unangetastet** — kein Neubau. NEU (und behalten) ist ausschliesslich der mobile Kanal + In-App-Impact-Feed + Student-Voice-Loop, on top von E4. **Messaging-Provider exakt wie die E2-`PaymentProvider`-Naht:** jeder externe Versand (WhatsApp/Telegram/Messenger/Push) läuft über `MessagingProvider` (Symbol-DI-Token `MESSAGING_PROVIDER`); Default = deterministischer `MockMessagingProvider` (kein Netz, In-Memory-Sent-Log, in ALLEN Tests + im Prototyp genutzt), echte `WhatsAppMessagingProvider`- (Graph Cloud API) + `TelegramMessagingProvider`-Skeletons (`fetch`, kompilieren, nie in Tests), gewählt durch die pure `createMessagingProvider`-Factory (`MESSAGING_PROVIDER=mock|whatsapp|telegram` + Credentials-Check, Fallback Mock → App bootet ohne Keys). **Wiederverwendung statt Neubau:** `voice-moderation` reust die E9-`ofac-keyword-matcher` (`normalizeText`/`matchSuspiciousKeywords`) + eine kleine Slur-Liste — kein zweiter Filter; `read-streak` füttert die Feed-Read-Timestamps in das E16-`computeMonthlyStreak` (Update-Read-Streak, kein zweiter Streak-Kern); der Feed wird aus den E4-`CampaignUpdate`-Meilensteinen (+ E4-`crossedMilestones`) + APPROVED Student-Voices projiziert (kein zweiter Update-Store, abgeleiteter State driftet nicht — E16-Linie). Nichts in E17 schreibt auf `Donation`/`Payout`; der Inaktivitäts-Reminder verlinkt nur den bestehenden Donate-Flow (Geld weiterhin nur an die Schule, Constitution II/III). **Pure, getestete Kerne (je Per-Path-80%-Gate, kein `Date.now()` — `now` injiziert):** `notification-timing` (Smart-Timing: Min-Intervall + Quiet-Hours, deterministisch, kein Spam, kein Cron), `channel-router` (In-App immer Ziel, Rest nur Opt-in; `messengerChannels` für die Provider-Versand-Kanäle mit Handle), `feed-builder` (Story-Cards aus update/milestone/voice + stabiler `feedItemKey` + chronologischer Sort newest-first), `voice-moderation` (E9-Reuse + Slur-Liste + Längen-/URL-Check → APPROVE/REJECT + Gründe), `inactivity` (letztes Spendendatum + Schwelle → inactive/daysSince/shouldRemind), `digest` (E-Mail-/Messenger-Digest-Aggregat), `read-streak` (E16-Wrapper), plus `mock-messaging.provider` + `messaging-provider.factory` + `impact-feed.service` gegated. Endpunkte: DONOR (`GET /feed`, `POST /feed/:itemKey/read`, `GET/PUT /feed/channel-prefs`, `GET /feed/inactivity`), STUDENT (`POST /campaigns/:id/voice` — Owner-skopiert, moderiert; APPROVE → `StudentMessage` gespeichert + an Opt-in-Spender über den gemockten Provider gefächert, REJECT → Gründe, kein Send); Boundary-Validation per class-validator-DTO, `{success,data}`-Envelope. Video/Voice sind URLs (kein Upload). Prisma-Migration `impact_feed` (Enums StudentMessageStatus/FeedChannel; Modelle StudentMessage [moderiert, text/videoUrl/voiceUrl]/NotificationChannelPref [Opt-in pro Donor+Kanal, `@@unique(userId,channel)`]/FeedRead [`@@unique(userId,feedItemKey)` Read-Dedupe + Streak-Quelle]; Rück-Relationen an User/Campaign; rein additiv — `FeedItem` ist ein reiner View-Typ, kein DB-Modell), Seed um eine moderierte (APPROVED) Student-Voice auf Amaras Kampagne + `donor@bursa.test`-Kanal-Präferenzen (WhatsApp+Telegram opt-in mit Handles, E-Mail an, Push aus) + eine 3-Monats-Read-Streak erweitert + idempotent. Frontend (in den E4/E16-`/donor`-Account integriert): swipebarer In-App-Impact-Feed (mobile-first Story-Cards mit Foto/Kind-Badge/Media-Hinweis/CTA + mark-as-read), Kanal-Präferenzen-Panel (Opt-in-Toggles WhatsApp/Telegram/Messenger/E-Mail-Digest/Push, IN_APP gesperrt), Read-Streak-Anzeige, Inaktivitäts-Reminder-Banner mit 1-Tap-Donate-CTA; Studi-Fläche "Send a thank-you message" (Text + optionale Video-/Voice-URL, Client-Vorvalidierung + Moderations-Hinweis); pure Helfer (`feed-card-format`/`channel-pref-format`/`read-streak-format`/`voice-message.helpers`) + vier Komponenten gegated. API 1179 Tests, Web 494 Tests grün, Per-Path-80%-Gates auf 10 Backend- (7 pure Kerne + Mock-Provider + Factory + Service) + 8 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected". Bewusste Out-of-Scope (ehrlich): kein echter WhatsApp-/Telegram-/Push-Versand (nur Mock; Real-Provider sind kompilierende Skeletons, nie in Tests), kein Scheduler/Cron-Worker (Timing on read/on trigger berechnet), Student-Video/-Voice sind URLs (kein Upload-/Transcoding-/Storage-Pipeline, 30s-Grenze deklariert), E4-E-Mail-Pfad unverändert, Slur-Filter = E9-Keyword-Heuristik (kein ML-Modell), kein A/B-Timing-Experiment-Framework (deterministischer Min-Intervall-/Quiet-Hours-Scheduler), Single-Instance (Read-Unique + Mock-Idempotenz als Garantien). Letztes Epic der Welle B (Roadmap v2) nach E8–E16.
- 2026-07-01: **E18 FERTIG** (PR offen, Welle 2 / Welle C — ERSTES Epic der Welle C). Gruppen-Engine als eigenes `groups`-Modul, gebaut nach dem Kern-Prinzip **EINE Engine, ZWEI Modi**: `Group.mode` (`COHORT` | `GIVING_CIRCLE`) diskriminiert Studierenden-Kohorten und Spender-Giving-Circles, die sich **dasselbe** Datenmodell + dieselbe Service-Logik teilen (Mitgliedschaft, Rollen, Shared-Goal, Leaderboard, Invites, Voting, moderierter Chat, Analytics) — kein zweiter Datenpfad. Der Modus steuert nur die Fläche (Cohort speist das Shared-Goal aus Sub-Kampagnen-`raisedCents`, Circle aus Mitglieder-Beiträgen). **Konsequente Wiederverwendung statt Neubau (kein neues Leaderboard-/Badge-/Aggregat-System):** `group-leaderboard` ist ein dünner Wrapper über das E16-`leaderboard.util.rankLeaderboard` (Team-/Circle-Boards, optional anonym "Member #n"), `group-analytics` + das Group-Portfolio wickeln das E16-`cumulative.util.aggregateContributions`, die Wochen-/Ranking-Badges nutzen E16-`badge.util.resolveTier`; `group-invite` reust das E15/E8-Einmal-Token-Muster (`referral-code.util`: 96-bit-Zufall, nur SHA-256-Hash gespeichert, timing-safe Validierung, injizierbare `bytes()`) — der Roh-Token steht nur im Invite-Link (QR = dieselbe URL), hash-only persistiert; `chat-moderation` reust die E9-`ofac-keyword-matcher` (`normalizeText`/`matchSuspiciousKeywords`) + eine Slur-Blocklist (dieselbe Linie wie der E17-`voice-moderation`, ein einziger Moderations-Filter im Repo); der **Cohort-Match läuft über den bestehenden E5-`CorporateService.sponsor`-Flow** je Sub-Kampagne (`GroupsModule` importiert `CorporateModule`, `CorporateModule` exportiert nun `CorporateService`) — **KEIN neuer Payment-Pfad**, E18 schreibt nie selbst auf `Donation`/`Payout` (Geld weiterhin nur an die Schule, Constitution II/III). `GroupContribution` ist ein money-freier Spiegel einer bestehenden Spende (Projektion für Aggregat/Leaderboard), kein zweiter Geld-Pfad. **Pure, getestete Kerne (je Per-Path-80%-Gate, kein `Date.now()` — `now` injiziert):** `membership` (Join/Leave/Rollen-State-Machine, letzter-Admin-Schutz, `cohortActive` ≥ 2, `canManage/canContribute/canVote`), `shared-goal` (summiert Teile gegen das Ziel → raised/percent/remaining, on read), `stretch-goal` (Unlock-Decision bei ≥ Schwelle [Default 80%], Recognition-only, kein Payout), `group-leaderboard` (E16-Wrapper), `voting` (Tally pro Option, stabiler Tie-Break über Options-ID, Quorum), `group-analytics` (E16-Wrapper + Mitglieder + Ziel-Fortschritt + ISO-Woche), `group-invite` (E15/E8-Wrapper + `decideInviteAcceptance`: Status/Ablauf/Mitgliedschaft), `chat-moderation` (E9-Reuse + Slur-Liste + Längen-Check), `cohort-match` (`splitCohortMatch`: money-freie Betrags-Aufteilung je Sub-Kampagne, GAP-proportional [kein Over-Funding] oder EVEN, deterministischer Remainder), plus `groups.service` gegated. Endpunkte (`@Controller('groups')`, JWT-gated, feingranulare Autorisierung über die **Gruppen-Rolle** ADMIN/CONTRIBUTOR/VIEWER im Service): CRUD + Übersicht (`POST/GET /groups`, `GET /groups/:id`), Membership + Invites (`POST /groups/:id/invites|join|leave`, `PUT …/members/:userId/role`), Cohort (`POST …/campaigns` owner-skopiert, `POST …/match` über E5), Circle (`POST …/contributions`, `GET …/analytics`), Voting (`POST …/votes`, `POST …/votes/:id/ballot`, `GET …/votes/:id`, `POST …/votes/:id/close`; ein Ballot/Mitglied), moderierter Chat (`POST/GET …/messages`, request/response, nur APPROVED sichtbar); Boundary-Validation per class-validator-DTO (inkl. nested Vote-Optionen), `{success,data}`-Envelope. Prisma-Migration `groups_engine` (Enums GroupMode/GroupVisibility/GroupRole/GroupInviteStatus/GroupVoteStatus/GroupMessageStatus; 9 Modelle Group/GroupMember [`@@unique(groupId,userId)`]/GroupInvite [hash-only]/GroupCampaign [`@@unique(groupId,campaignId)`]/GroupContribution [`@@unique(groupId,donationId)`, money-frei]/GroupVote/GroupVoteOption/GroupVoteBallot [`@@unique(voteId,userId)`]/GroupMessage [moderiert]; Rück-Relationen an User/Campaign/Donation; rein additiv — SharedGoal/Stretch/Leaderboard/Analytics sind reine View-Typen, keine DB-Modelle), Seed um eine Cohort "INSEAD MBA 2026 Cohort" (3 Sub-Kampagnen + einen Corporate-Match) + eine Giving Circle "Lagos Alumni Giving Circle" (3 Mitglieder [Admin/Contributor/Viewer] + 2 money-freie Beiträge + ein offenes Voting mit Ballot + eine moderierte [APPROVED] Nachricht) erweitert + idempotent (`clearDatabase()` um die 9 Tabellen ergänzt). Frontend (`apps/web/src/app/features/groups/` + neue `/groups`-Route auth-gated für alle Rollen + Navbar-Link): Groups-Landing (meine + öffentliche Gruppen, Create-Group-Form mit Modus-Wahl), Group-Detail (gemeinsame Progress-Bar, Stretch-Goal-Anzeige, Mitglieder mit Rollen, E16-Leaderboard, Sub-Kampagnen [Cohort] / Portfolio [Circle]), Voting-UI (Optionen mit Stimmenanteil, abstimmen, Tally), moderierter Chat (posten + Historie + Moderations-Hinweis); pure Helfer (`group-format`/`role-format`/`voting-format`) + drei Komponenten gegated; `models.ts` prettier-formatiert (war auf `main` von der Prettier-Norm abgedriftet). API 1292 Tests, Web 528 Tests grün, Per-Path-80%-Gates auf 10 Backend- (9 pure Kerne + `groups.service`) + 6 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected", `prettier --check` clean (beide Apps inkl. `models.ts`). Bewusste Out-of-Scope (ehrlich): kein Echtzeit-WebSocket-Leaderboard (rankLeaderboard on read), kein Live-Chat-Socket (moderiertes request/response), Single-Instance (DB-Uniques als Dedupe), Cohort-Match = bestehender E5-Flow (kein neuer Payment-Pfad, keine Cohort-eigene Zahlungslogik), Chat-/Voice-Moderation = E9-Keyword-Heuristik (kein ML-Modell), keine Cash-Rewards an Badges/Stretch-Goals (Recognition/Feature-only, Compliance), QR = dieselbe Invite-URL (kein serverseitiger QR-Generator), kein Group-eigenes Recurring/Payout (Gruppen money-frei). Erstes Epic der Welle C (Roadmap v2), erster Consumer der E16-Gamification-Primitive UND der E15/E8-Invites UND der E9-Moderation UND des E5-Corporate-Flows in einem Modul.
- 2026-07-01: **E19 FERTIG** (PR offen, Welle 2 / Welle C — Enterprise). Self-Serve Corporate Scholarship Program Manager (Größe XL) als eigenes `scholarship`-Modul: eine Firma (SPONSOR) fährt ihr eigenes gebrandetes Stipendienprogramm — schema-getriebenes Application-Formular mit Conditional-Logic, Reviewer-Scoring mit Konsens-Rubric, Award-Kürung, **Auto-Disbursement an die Schule** (nie an den Scholar), Scholar-Relationship-Management (SRM), Impact-Report und Multi-Cycle-Renewal. **Konsequente Wiederverwendung statt Neubau (Constitution II — Geld immer an die Schule):** Award-Auszahlung läuft über den bestehenden **E2-`PaymentProvider.createPayout`** (`schoolName`/`accountRef` der verifizierten Schule) und wird als `entryType: DISBURSEMENT` in das **append-only E12-`LedgerService`** geschrieben (`LedgerService.append`, per-School-Hash-Chain) — kein zweiter Payment-Pfad, kein zweites Ledger; die Conditional-Tranche (Semester 2 bei GPA-Threshold) zahlt ebenfalls an die Schule; Diversity-Metriken des Impact-Reports kommen aus dem **E14-`aggregateDiversity`**, PDF/CSV aus dem **E5-`buildSimplePdf`** + `cell()`-Escaping, Scholar-SMS/E-Mail über die **E17-`MessagingProvider`**-Naht (Symbol-DI-Token `MESSAGING_PROVIDER`, `MockMessagingProvider` Default, env-gegatete Factory), Application-/Status-Links über das **E8/E11/E14-Token-Muster** (`application-token`: 24-Byte-Zufall, nur SHA-256-Hash gespeichert, timing-safe); der Ledger wird nur read-appended, nie mutiert. **Pure, getestete Kerne (je Per-Path-80%-Gate, kein `Date.now()` — `now` injiziert, neue Objekte, keine Mutation):** `form-schema.validator` (eindeutige Keys, SELECT braucht Optionen, Conditional-Ref existiert), `conditional-logic` (`evaluateVisibility` — deterministische `showIf`-Auswertung), `answer.validator` (Pflicht/Typ/SELECT, unsichtbare Felder übersprungen), `rubric-aggregator` (gewichteter Mittelwert je Feld → 0-100 Konsens), `award-decision` (Ranking + Budget-/Slot-Cutoff, Ties nach `appId`), `conditional-disbursement` (GPA-Threshold → RELEASE/HELD), `scholar-status` (State-Machine AWARDED→ENROLLED→GRADUATED→WORKING + WITHDRAWN, ungültige Übergänge werfen), `program-cycle` (`planRenewal` — Jahr+1, Schema-Kopie), `outcome-aggregator` (Retention-/Graduation-Rate, Alumni), `application-token` (hash/isActive/timing-safe match), `report-builder` (Outcome+Diversity → CSV/PDF-Zeilen), plus `scholarship.service` gegated. Endpunkte: SPONSOR (`@Controller('scholarship')`, JWT+Roles, Owner-skopiert) — Programm-CRUD + Branding, `PUT …/form` (Schema), Reviewer (≤10), `POST /applications/:id/scores`, `POST …/decide` (Gewinner küren), `POST /awards/:id/disburse` (an die Schule), `POST /awards/:id/release-tranche` (GPA-Threshold), SRM (`GET …/scholars`, `PUT /scholars/:id/status`, `POST /scholars/:id/message`), Impact-Report (`GET …/report.csv|.pdf` via `@Res()`), Renewal; öffentlich token-gegatet (`@Controller('apply')`, kein Login) — `GET/POST /apply/:token`, `GET …/status`; Boundary-Validation per class-validator-DTO, `{success,data}`-Envelope. Prisma-Migration `scholarship_manager` (Enums FieldType/ApplicationStatus/ScholarStatus/AwardTrancheStatus; 10 Modelle ScholarshipProgram/ProgramCycle [`@@unique(programId,year)`]/ApplicationForm/FormField [`@@unique(formId,fieldKey)`]/Application [`tokenHash @unique`]/ApplicationAnswer/ProgramReviewer/ReviewScore/ScholarshipAward [+Conditional-Tranche]/ScholarRelationship; Rück-Relationen an CorporateProfile/School/User; Ledger lose über `refType`/`refId` referenziert, kein FK; rein additiv), Seed um ein gebrandetes Demo-Programm des Sponsors "Acme Future Leaders Scholarship" (Formular mit Conditional-Feld, 3 Applications + Reviewer-Scores, 1 Award an ESMT DISBURSED + Ledger-Eintrag + HELD-Zweit-Tranche, 1 SRM-Scholar [ENROLLED] mit Diversity-Linkage) erweitert + idempotent (`clearDatabase()` um die 10 Tabellen child-first ergänzt). Frontend (`apps/web/src/app/features/scholarship/` + neue Routen `/scholarship` SPONSOR-gated + öffentliche `/apply/:token`): Programm-Admin-Seite (Programm anlegen/branden, Award-Management mit Ranking + "Decide winners", SRM-Dashboard mit Status-Advance + Messaging, CSV/PDF-Export, Renewal), Reviewer-Konsole (Rubric-Scoring), öffentliches Bewerbungsformular mit clientseitiger Conditional-Sichtbarkeit (spiegelt den Backend-Kern); pure Helfer (`field-format`/`status-format`/`apply-visibility`) + 3 Komponenten (`srm-dashboard`/`reviewer-console`/`award-management`) gegated. API 1413 Tests, Web 567 Tests grün, Per-Path-80%-Gates auf 12 Backend- (11 pure Kerne + `scholarship.service`) + 6 Frontend-Cores/Komponenten, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected", `prettier --check` clean (beide Apps). Bewusste Out-of-Scope (ehrlich): kein Drag-Drop-WYSIWYG-Builder (schema-getriebene Feld-Liste), kein echter SMS-/E-Mail-Versand (nur Mock-Messaging; Real-Provider kompilierende Skeletons), kein HRIS-/Alumni-CRM-Sync (SRM-Felder manuell), Conditional-Payouts = Prototyp-Schwellwert-Logik auf manuell gepflegtem GPA (kein Registrar-Feed), Branding = Slug+Logo+2 Farben (kein White-Label/DNS/Custom-CSS), kein Bewerber-Account (anonymer Token-Link), kein Firmen-Budget-Zahlungseingang (nur Award-Auszahlung an die Schule via E2/E12; wie die Firma zahlt ist E5-Terrain), Single-Instance (Ledger-Append serialisiert pro Schule wie E12). XL bewusst als kohärenter End-to-End-Slice geliefert statt jede Sub-Feature zu vergolden. Enterprise-Epic der Welle C (Roadmap v2) nach E18.
- 2026-07-01: **E20 FERTIG** (PR offen, Welle 2 / Welle C — Enterprise & Skalierung). Multi-Currency & lokale Zahlungsmethoden (Größe XL) als eigenes `fx`-Modul + `payments/local`-Provider-Naht. **Modell-Korrektur (harte Regel, Constitution II):** Auszahlung erfolgt in Landeswährung (KES/NGN/GHS/BDT/PHP/VND) **an die verifizierte SCHULE, nie an Studierende** — es gibt kein Studenten-Wallet, keinen Scholar-Payout in keiner Währung; die lokalen Methoden (M-Pesa, Mobile Money, GCash, bKash, lokale Banküberweisung) sind ausschließlich für die **Spender-Einzahlung (Donor-Deposit)**. **Konsequente Wiederverwendung statt Neubau:** Der Schul-Payout läuft über den bestehenden **E2-`PaymentProvider.createPayout`** und wird als `entryType: DISBURSEMENT` (mit `currency`) in das **append-only E12-`LedgerService`** geschrieben (kein zweiter Payment-Pfad, kein zweites Ledger — `MovementInput.currency` war bereits multi-currency-fähig); die lokalen Deposit-Provider sind zusätzliche `PaymentProvider`-artige Implementierungen hinter einem eigenen Seam (`LOCAL_DEPOSIT_PROVIDER`-Symbol + env-gegatete Factory `createLocalDepositProvider`, `MockLocalDepositProvider` Default, `MpesaDepositProvider` kompilierendes Real-Skeleton) — 1:1 die `createPaymentProvider`-Linie; der FX-Seam analog (`FX_RATE_PROVIDER`-Symbol, `MockFxRateProvider` mit USD-Hub-Tabelle, `createFxRateProvider`); der Local-Payment-Webhook nutzt den **E6-`verifyWebhookSignature`**-Guard (HMAC-SHA256 über Raw-Body, fail-closed 400 INVALID_SIGNATURE, eigener Header `x-local-payment-signature`); das Schul-Payout-Konto hängt an **E8-`School`** (`SchoolPayoutAccount` pro Land/Währung); die Per-Land-KYC-Variation speist die **E11**-Pipeline (`resolveKycRequirement`: BVN/NG, NATIONAL_ID/KE-GH, PASSPORT-Default), baut sie nicht neu. **Pure, getestete Kerne (je Per-Path-80%-Gate, keine Floats für Geld, `now`/Kurse injiziert, neue Objekte, keine Mutation):** `currency` (Registry mit `decimals`/`symbol`), `money-minor-unit` (`toMinorUnits`/`roundHalfUp`, integer-safe), `currency-converter` (`convertMinorUnits` locked-rate, round-half-up mit Float-Drift-Korrektur, gleiche Währung = No-Op), `fx-slippage` (Minor-Units + Basispunkte + Richtung), `locked-rate` (`quoteLockedRate` aus injizierter Tabelle, inverse Paare), `payment-method-resolver` (Land → lokale Methoden + Card-Fallback), `payout-routing` (`decidePayoutRoute` LOCAL_BANK vs INTERNATIONAL), `i18n-labels` (`resolveLabels` en/sw/yo/bn/tl mit en-Fallback), `local-bank-detail` (`validateVirtualIban` Mod-97/ISO-13616 Prüfsumme, display-only), `country-kyc-requirement`; plus `mock-fx-rate.provider`, `local-payment.provider.factory`, `local-payment-webhook.guard`, `fx.service`, `fx.controller`, `mock-local-payment.provider` gegated. Endpunkte (`@Controller('fx')`): öffentlich `GET /fx/currencies|quote|methods|labels`, `POST /fx/deposits` (lokaler Deposit → PENDING-Referenz, Kurs fixiert, Schul-Betrag konvertiert), `POST /fx/webhook` (signatur-geprüft, PENDING→SUCCEEDED/FAILED); SCHOOL_ADMIN/ADMIN `POST/GET /fx/school-accounts`; ADMIN `POST /fx/payouts` (Routing → `createPayout` an die Schule → Ledger-`DISBURSEMENT`). Prisma-Migration `multi_currency` (Enums Currency/LocalPaymentMethod/PayoutRoute/LocalDepositStatus; Modell `SchoolPayoutAccount` [`@@unique(schoolId,country,currency)`]; Donation-Felder depositCurrency/depositMethod/lockedRate/payoutCurrency/localDepositRef/localDepositStatus; rein additiv), Seed um eine KES-Demo (ESMT lokales KES-Konto + Virtual-IBAN, M-Pesa-Spende USD→KES @129.5 mit fixiertem Kurs, KES-`DISBURSEMENT`-Ledger-Eintrag an die Schule) erweitert + idempotent (`clearDatabase()` um `SchoolPayoutAccount` child-first ergänzt). Frontend (`apps/web/src/app/features/donate/` + `features/school-settings/` + neue Routen `/donate/:campaignId/local` öffentlich + `/school/currency` SCHOOL_ADMIN-gated): lokalisierter Donate-Flow (Land/Währung/Methode-Auswahl, Swahili-/Yoruba-/…-Labels, FX-Anzeige "Du zahlst X → Schule erhält Y"), Schul-Currency-Settings (lokales Auszahlungskonto), pure Helfer (`currency-format`/`fx-display`/`method-labels`/`i18n-resolve`, Spiegel der Backend-Kerne) gegated. API 1525 Tests, Web 586 Tests grün, Per-Path-80%-Gates auf 17 Backend- + 4 Frontend-Cores, beide Builds grün, Seed läuft, `prisma migrate status` up to date + `diff --exit-code` "No difference detected", `prettier --check` clean (beide Apps). Bewusste Out-of-Scope (ehrlich): Auszahlung an die SCHULE (nicht an Studierende) in jeder Währung, kein Studenten-Wallet; keine echten Gateways (M-Pesa/Mobile-Money/GCash/bKash gemockt, Real-Provider Skeleton); nur Mock-FX (injizierte Tabelle, kein Live-Feed/echtes Hedging/FX-Matching); i18n = Sample-Labels (keine Voll-Übersetzung, kein ICU/RTL); Virtual-IBANs display-only (Format+Prüfsumme, keine echte Bank-Verifikation); kein Live-Sanktions-/AML-Feed über den E11-Mock hinaus; kein Multi-Currency-Wallet/Treasury/Netting; länderspezifische Steuer-/Spendenquittungs-Logik bleibt bei E12/E14. XL bewusst als kohärenter End-to-End-Slice geliefert (Donor-Deposit → FX-Freeze → Schul-Payout in Landeswährung → Ledger) statt jede Sub-Feature zu vergolden. Enterprise-Epic der Welle C (Roadmap v2) nach E19.
